<form>
    <div class="container-fluid m-2">
        <div class="row">
            <div id="boton" class="col-4">
                {# Botón para acceder a la biblioteca #}
            </div>
            <div id="busqueda" class="col-4">
                {# Cuadro de búsqueda y botón #}
                <div class="row">
                    <div class="col-8 p-0">
                        <input id='txtBuscar' type="text" name="txtBusqueda" class="form-control" placeholder="Buscar">
                    </div>
                    <div class="col-4 p-0">
                        <input id='btnBuscar' type="button" name="btnBuscar" class="btn btn-primary" value="Buscar">
                    </div>
                </div>
            </div>
            <div id="usuario" class="col-4">
                {# Cuenta del usuario #}
            </div>
        </div>
    </div>
</form>
