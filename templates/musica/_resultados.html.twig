<h4>Resultados de la búsqueda</h4>
{% for cancion in canciones  %}

        <div id="pista" class="d-flex justify-content-between bg-primary p-1 hover">
            <div id="informacion">
{#                <span class="text-uppercase" name="artista">{{ cancion.artista }}</span>#}
                <span name="titulo">{{ cancion.url }}</span>
            </div>
            <div>
                <a href="./assets/audio/{{ cancion.url }}">
                    <i class="fas fa-play-circle px-1"></i>
                </a>
                <i class="far fa-heart px-1"></i>
                <i class="fas fa-plus px-1"></i>
            </div>
        </div>
    
{% else %}
    No hay elementos, el campo de búsqueda estaba vacío.
{% endfor %}